repos:
- repo: local
  hooks:
  - id: commit-msg-check
    name: Check commit message format
    entry: ./scripts/check-commit-msg.sh
    language: system
    pass_filenames: true
    stages: [commit-msg]